[project]
name = "project"
version = "0.1.0"
description = "Road lanes detection"
requires-python = ">=3.12"
authors = [{ name = "memothelemo", email = "dev@memothelemo.xyz" }]
dependencies = [
    "numpy>=2.5.3",
    "opencv-contrib-python>=5.0.0.93",
]

[project.scripts]
project = "main"
